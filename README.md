# video-index

This is a web-based application to keep track of video files on your local disk.

## Module dependencies

```mermaid
graph TD
    a(app)
    m(models)
    t(templates)
    v(versions)

    a --> m
    a --> t
    t --> m
    t --> v
```
